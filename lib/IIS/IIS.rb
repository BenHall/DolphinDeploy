class IIS
  def deploy(server, location, deployment)
    #If server == IIS6 then
    #iis = IIS6.new
    #else
    #iis = IIS7.nwe
    
    #iis.create(server, location, deployment)
  end
end